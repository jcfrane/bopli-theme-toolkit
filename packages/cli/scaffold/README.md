# Bopli theme

This project was generated by `bopli-theme create`.

```sh
npm install
npm run dev
```

The standalone preview uses `resources/bopli/starter.json`. Run `npm run dev:app` when you need to verify the theme against a local Bopli application.
